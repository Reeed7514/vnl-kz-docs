# Binds & Aliases
## jump bug
```
alias +jb "+jump; -duck; -forward";
alias -jb "-jump";
bind <key> +jb
```
## forward release
```
alias +lj "+duck; +jump; -forward";
alias -lj "-duck; -jump";
bind <key> +lj
```